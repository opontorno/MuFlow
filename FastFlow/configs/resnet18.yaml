input_size: 256
backbone_name: resnet18
flow_step: 8
hidden_ratio: 1.0
conv3x3_only: True
pooling_type: flatten # choices: 'mean', 'flatten', 'max', 'mean_std', 'full_flatten'
gmm_n_components: 1
out_indices: [1, 2, 3]  # Feature extraction layers (e.g., [1,2,3] or [0,1,2,3,4])